{
  "name": "@cpt-ui-common/testing",
  "version": "1.0.0",
  "description": "Common resources for testing prescription status update",
  "author": "NHS Digital",
  "license": "MIT",
  "main": "lib/src/index.js",
  "type": "module",
  "scripts": {
    "unit": "jest",
    "lint": "eslint  --max-warnings 0 --fix --config ../../../eslint.config.mjs .",
    "compile": "tsc --build",
    "test": "npm run compile && npm run unit"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^2.32.0"
  },
  "devDependencies": {
    "@cpt-ui-common/common-types": "^1.0.0",
    "axios": "^1.14.0"
  }
}
